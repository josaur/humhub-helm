{{- if (not .Values.externalDatabase.existingSecret)  }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "humhub.databaseSecretName" . }}
type: Opaque
data:
  username: {{ .Values.externalDatabase.user }}
  password: {{ .Values.externalDatabase.existingSecret | b64enc | quote}}
{{- end }}